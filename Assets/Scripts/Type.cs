public enum CharacterType
{ 
    Player, 
    Enemy 
}

public enum PoolType
{
    Arrow,
    StraightArrow,
    JumpShotArrow,
    Explosion
}

public enum SkillType
{
    StraightArrow,
    JumpShot,
    Fireball,
}
