public enum CharacterType
{ 
    Player, 
    Enemy 
}

public enum PoolType
{
    Arrow,
    StraightArrow,
    FlyArrow,
    Explosion
}

public enum SkillType
{
    StraightArrow,
    JumpShot,
    Fireball,
}
