public enum CharacterType
{ 
    Player, 
    Enemy 
}

public enum PoolType
{
    Arrow,
    StraightArrow,
    JumpShotArrow,
    Shield,
    ShieldHpBar,
    FireArrow
}

public enum SkillType
{
    StraightArrow,
    JumpShot,
    Fireball,
}

public enum OwnerType
{
    Player,
    Enemy
}
